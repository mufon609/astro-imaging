# Fresh-session prompt — process july31 from raw frames

---

`sessions/july31/` holds four light sets and a darks group. **Raw frames only** —
every master, intermediate, stack, render and per-dataset record from previous work
on this session has been removed.

Take it through to finished, judged products.

Read `CLAUDE.md` first. It states what this repo is, the binding rules, and the
order to read everything else in. That is the whole briefing — the repo carries the
rest, including what has already been tried and why.

Work the way the contract says: measure the data with the tools, match it to a
route, tell me what you recommend and why, and wait where the decision is mine.

Report what you measured, what you chose, what you rejected, and anything that
looks wrong or that you could not explain. A clean null is a result. Nothing is
"fixed" or "final" until it is measured, and aesthetics are my eyes on the finals.

I am here for the decisions the contract reserves for me — ask, and I will answer.
