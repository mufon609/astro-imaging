requires 1.4.0
set32bits
cd work
# Color is calibrated by SPCC on the stack — no per-channel white balance
# at stack time (-rgb_equal), same reasoning as the matched-flat lights stack.
stack r_pp_bkg_pp_light rej 3 3 @STACKPOL@-norm=addscale -output_norm -out=../results/stack_@SET@
close
