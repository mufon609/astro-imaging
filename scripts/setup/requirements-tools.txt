# Pinned Python TOOLS layer — installed by scripts/setup/install_python_tools.sh
# into the project venv ($ASTRO_VENV, default /opt/astro-venv). NEVER system pip
# (Kali enforces PEP 668).
#
# SEPARATE FROM requirements.txt ON PURPOSE. requirements.txt is the MEASUREMENT
# layer the repo's own scripts import (astropy/numpy/scipy/PIL) — breaking it
# breaks the chain. This file is the TOOL layer: candidate instruments answering a
# named open question that nothing on the build path imports yet. Keeping them
# apart means a tool experiment cannot take the chain down with it.
#
# EVERY ROW RECORDS A QUESTION, NOT A CAPABILITY — and the distinction is the point.
# An installed tool nobody drives is debt; a capability sitting unnoticed is the
# opposite failure and this repo has measured it repeatedly (`seqtilt`, SWarp's
# weight map, `grid_ramp.py`). Name the question, and delete the row when it closes.
#
# INSTALLED IS NOT INTEGRATED, AND THIS FILE MUST NOT READ AS THOUGH IT WERE.
# MEASURED 2026-08-14, immediately after installing: `grep -rl "import <tool>"`
# across `scripts/`, `web/` and `datasets/` returns NOTHING for any of them. So on
# the day they landed, every row below was RECORDED-ONLY. That is the normal state
# after a procurement pass and it is not a defect — **the defect would be a future
# reader taking "we have X installed" as capability.** Status, honestly:
#   ACTING NOW      sip_tpv   — verified, and it gates a live candidate route
#   ALREADY IN USE  PSFEx     — its output is already cited; the install closed a
#                               reproducibility gap rather than unlocking anything
#   ON NEXT USE     galsim    — inert today, but one named use fires the moment
#                               anyone reads a PSFEx .psf again
#   INERT           SCAMP, reproject, piff, astropy_healpix, astroquery — no arm
#                               designed for any of them yet
# **Carry a LAST-USED stamp beside the last-checked one.** A tool that has been
# installed for months and never imported is a stale claim waiting to happen, and
# nothing else in this file would catch it.
#
# ===========================================================================
# THERE ARE EXACTLY TWO INSTALL LANES ON THIS RIG, AND NO THIRD — MEASURED.
# `make` and `gcc` are present; `autoconf`, `automake` and `libtool` were ABSENT
# and installing them needs root. So autotools-based Debian sources — PSFEx,
# SCAMP — cannot be built from `apt-get source` **WITHOUT ROOT**, however
# reachable the source package is.
# **THAT CONDITION HAS SINCE BEEN MET AND THE SENTENCE ABOVE ONCE OMITTED IT.** It
# read "CANNOT be built … however reachable the source package is", which a reader
# takes as a standing impossibility rather than a one-time prerequisite — and the
# owner then ran the root-once build-dep install and BOTH ARE NOW BUILT AND
# RUNNING (`scamp -v` → 2.10.0, `psfex -v` → 3.21.1, from ~/.local/bin). Recorded
# rather than silently corrected because it is this file's own lesson at
# one-day scale: a constraint stated without its condition goes stale the moment
# the condition changes, and nobody re-reads it. The two lanes are:
#   (a) PyPI       pin `name==version`; capture artifact sha256 via
#                  `pip install --dry-run --report`.
#   (b) Debian .deb  pin the .deb sha256 against the archive `Packages` index plus
#                  transitive deps, extract, run via `LD_LIBRARY_PATH`.
# Stated here so the next person does not try `dpkg-buildpackage`, hit the same
# wall, and rediscover it.
# ===========================================================================
#
# MEASURED COSTS (`pip install --dry-run`, 2026-08-14). The pinned MEASUREMENT
# layer is NOT moved by any of these — astropy 8.0.1, numpy 2.3.5, scipy 1.17.1
# and Pillow 12.2.0 all resolve "already satisfied".
#
#   sip_tpv            3      galsim             5      astropy_healpix    2
#   piff              13      reproject         20      torchmfbd         37

# ===== INSTALLED =============================================================

# --- Gates a live route, and the route is the largest measured defect we have.
# SWarp CANNOT read SIP: a 3-char compare truncates RA---TAN-SIP to TAN, and its
# distortion gate needs PV terms SIP does not carry, so it applies nothing and
# warns about nothing. MEASURED from source AND confirmed by SWarp itself — an
# identical output canvas and CRVAL to nine decimals from TAN-SIP and from
# SIP-stripped, while TPV moves it 22x8 px. sip_tpv's FORWARD direction is a
# symbolic sympy substitution, not a fit: exact to 1.118e-11 px max over 3600
# points and FLAT in field radius, against a distortion-stripped positive control
# reading 13.82 px and growing to the corner. Only route around the blindness.
sip_tpv==1.1

# --- Pairs with PSFEx and retires a documented transcription trap.
# `galsim.des.DES_PSFEx` reads a PSFEx `.psf` directly and evaluates the PSF at an
# arbitrary position. TOOLS.md records that PSFEx exposes NO position-resolved
# shape, so any comparison must re-derive one — and that its polynomial basis order
# is [1, X, X2, Y, XY, Y2], not the order a reader assumes, which transposes the
# whole field model if taken wrong. GalSim does that re-derivation as a maintained
# library, so the trap cannot arise. Wheel, no C++ build.
galsim==2.8.5

# --- HARD PIN, AND WITHOUT IT sip_tpv DOES NOT IMPORT AT ALL.
# `sip_tpv/__init__.py:3` does `import pkg_resources` at module load, purely to
# populate `__version__`. setuptools REMOVED pkg_resources at 81, and galsim pulls
# setuptools 84 transitively — so installing the two together yields
# `ModuleNotFoundError: No module named 'pkg_resources'` and a package that
# installs cleanly and cannot be imported. MEASURED here: the install reported
# "Successfully installed … sip_tpv-1.1 …" and the import check then went RED,
# which is the whole reason that check exists. setuptools' own deprecation warning
# states the fix verbatim: "pin to Setuptools<81".
setuptools==80.9.0

# --- PINNED EXPLICITLY BECAUSE IT IS DATE-VERSIONED AND ARRIVES TRANSITIVELY.
# `astropy-iers-data` republishes continuously (installed here: 0.2026.7.13.0.54.2;
# PyPI current at time of writing: 0.2026.8.10.0.32.39). It is a transitive
# dependency of astropy and therefore of nearly everything above, which is exactly
# why it gets left unpinned — and an unpinned date-versioned package means the
# environment drifts on every rebuild. Pinning it is what makes "reproducible from
# a clone" true rather than nearly true.
astropy-iers-data==0.2026.7.13.0.54.2
# DO NOT BUMP THIS REFLEXIVELY. The pin is a month behind PyPI's current by
# design — it is the version this venv was TESTED with, and pinning what you
# tested is the whole point. IERS data governs UT1-UTC and polar motion at the
# MILLIARCSECOND level; this corpus works at ~17 arcsec/px, so the staleness costs
# nothing measurable and bumping it to "fix" the date breaks reproducibility to
# solve a non-problem.


# --- Headless catalogue client — Vizier / SIMBAD / Gaia via pyvo.
# We hit exactly this wall: Siril's `conesearch` is GUI-ONLY headless (it aborts
# unconditionally in a script) and separately timed out on a 20.6 deg cone against
# TAPVizieR, so the workaround became astrometry.net's `--tag-all`, which only ever
# returns what the index happens to carry. This is the tool that makes that wall a
# non-event. Caveat kept so the package count is honest: it drags keyring/jeepney,
# a D-Bus credential stack that is dead weight headless but harmless.
astroquery==0.4.11

# --- Retires the nside=2 nested cover `spcc_cone.py` hand-rolls, which is a
# declared divergence with a removal condition. Debt reduction rather than new
# capability — spcc_cone.py works — but it is 2 packages and closes a register row.
astropy_healpix==2.0.1

# --- The astropy-native reprojector, and unlike SWarp it CONSUMES SIP directly
# through astropy's WCS, so it needs no conversion step at all. That makes it the
# independent second arm for BACKLOG:compose-homography-smear rather than only a
# fallback for the branch where the TPV route fails.
reproject==0.21.0

# --- DES Y3's replacement for PSFEx, modelling the PSF in SKY rather than pixel
# coordinates. That is precisely this corpus's axis: the PSF is fixed in SENSOR
# coordinates while the sky drifts ~1000 px across it, so which frame the model
# lives in is the whole union-vs-member question.
piff==1.6.0

# ===== NAMED, NOT INSTALLED — each with the trigger that would change it =====
#
#   PSFEx / SCAMP            NOT PyPI candidates — lane (b), and both need root
#       ONCE for build deps. See scripts/setup/install_astromatic.sh.
#       PSFEx is the strongest "what are we missing": not a tool nobody thought to
#       want, but one ALREADY IN USE whose output is ALREADY CITED in our records —
#       the field model that independently confirmed the corner degradation
#       (FWHM 1.95 px centre to 3.2 px corner, a different algorithm sharing no
#       code with Siril) and the degree-2-suffices result. It exists today only as
#       a scratchpad extraction of a Debian binary run through LD_LIBRARY_PATH:
#       the same gap sip_tpv had — verified and NOT reproducible from a clone — in
#       a tool that has already produced load-bearing findings.
#       SCAMP's photometric mode is a scalar per exposure and cannot satisfy the
#       object_tilt condition that names it — but that judged the WRONG HALF. Its
#       ASTROMETRIC side writes PV%d_%d keywords and carries TPV in its pcode list
#       (measured in src/fitswcs.c), i.e. SCAMP is the NATIVE PRODUCER of exactly
#       the format SWarp reads and that sip_tpv converts our SIP into. That is the
#       canonical Astromatic chain — SExtractor -> SCAMP -> SWarp, all speaking TPV.
#
# ===== ARGUED AGAINST ========================================================
#
#   properimage   CANNOT BE INSTALLED — `Failed to build 'tinynpydb' when getting
#                 requirements to build wheel`. Recorded because an earlier note
#                 called it "available on PyPI", which conflated AVAILABILITY with
#                 INSTALLABILITY. Different facts.
#   torchmfbd     37 packages including `torch` and `triton` — triton is the GPU
#                 kernel compiler, which is what the package is oriented at, and
#                 this rig has no NVIDIA GPU. Its three open checks are
#                 DOCUMENTATION checks, not install checks; do not install to find
#                 out. Chief among them: MOMFBD separates aberration from object
#                 using frame-to-frame aberration DIVERSITY, and ours is static
#                 and identical in every frame.
#   pypher        2 packages and light, but its only pipeline use is PSF
#                 homogenisation, which is OWNER-REFUSED as a category. Worth
#                 remembering for one thing it is NOT refused for: a matching
#                 kernel between two MEASURED PSFs characterises the difference
#                 without treating anything, and diagnostics sit outside the
#                 bright line. A reason to know it exists, not to install it.
#   scamp         ARGUED AGAINST ON ITS PHOTOMETRIC MODE ONLY, AND IT IS NOW
#                 BUILT — do not read this row as "unavailable". Photometric mode
#                 solves a SCALAR per exposure per instrument (`src/preflist.h`
#                 has no photometric analogue of DISTORT_DEGREES), so it cannot
#                 satisfy the position-dependent condition in `object_tilt.py`'s
#                 removal-conditions row. That half stands. **The "cannot be built
#                 here anyway" clause that used to close this row is DELETED — it
#                 is false, and it contradicted two other sections of this same
#                 file.** Its ASTROMETRIC side is a different question and is the
#                 reason it was installed: SCAMP is the native producer of TPV
#                 (`PROJECTION_TYPE  SAME # SAME, TPV or TAN`), which is the
#                 format SWarp reads. See BACKLOG:`compose-homography-smear`.
